GEMINI_API_KEY=PLACEHOLDER_API_KEY

# Frappe site origin (used by Vite proxy, and for building absolute file URLs)
VITE_FRAPPE_URL=https://dev.galaxylabs.online


# Optional: if you deploy the SPA on a different domain, set the full API base here.
# Otherwise leave blank and call relative /api via same-origin hosting or dev proxy.
VITE_API_BASE=
